# hide-obsolete-files
Drupal module for hiding public files with usage in older (unpublished) entity revisions by obfuscating filenames.

# Usage
- `hof:hide` to obfuscate filenames of obsolete files
- `hof:revert` to restore all filenames.
